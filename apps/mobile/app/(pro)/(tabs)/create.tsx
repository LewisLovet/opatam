/**
 * Create Tab Placeholder
 * This screen is never shown — the tab press redirects to create-booking.
 * Required by expo-router to register the tab route.
 */

import { View } from 'react-native';

export default function CreatePlaceholder() {
  return <View />;
}
