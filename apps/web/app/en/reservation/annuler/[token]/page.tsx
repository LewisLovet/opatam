// English URL for the cancellation page — thin re-export, see
// app/en/reservation/confirmation/[id]/page.tsx for the pattern.
export { default, generateMetadata } from '../../../../reservation/annuler/[token]/page';
